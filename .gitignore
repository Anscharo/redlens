# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Atlas-derived artifacts — built ephemerally at container startup / by the
# in-process updater / by the history worker. Never committed.
# Keep: public/addresses.json (Etherscan), public/chain-state.json (RPC),
#        public/processes*.json (curated), public/favicon.svg etc. (static).
public/docs.json
public/docs-shallow.json
public/docs-deep.json
public/search-index.json
public/relations.json
public/graph.json
public/glossary.json
public/addresses.atlas.json
public/manifest.json
public/history/
public/atlas/
# NOTE: the HTML-era curation pipeline artifacts (history-curation.json,
# history-auto-decisions.json, history-curation-proposals.json, history-decisions.json,
# history-html-era.json) are NO LONGER ignored — they are the committed, reviewed
# reconstruction (plan §7/§10.4). See the "committed curation pipeline" block below.



# Intermediate handoff file from build:index → build:addresses
public/addresses.merged.json

# Vector index — built locally (transformers.js bge-base-en-v1.5).
# vectors.bin tracked via Git LFS; CI reuses this via incremental hash check.
# .cache/atlas-vectors/ is committed, not ignored.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
note.md

# Playwright E2E artifacts
test-results/
playwright-report/
playwright/.cache/

# Lighthouse reports
.lighthouse-report*

.cache/pr-check/*
.cache/pr-check/pr227-ffe21c9.md
.cache/atlas-rag/chunks.json
.cache/atlas-rag/meta.json
.cache/atlas-rag/vectors.bin
.cache/processes-audit.json
.cache/audit-html-cases.json
.cache/audit-html-report.json
.cache/forward-reverse-diff.json
.cache/processes-audit.md
.cache/processes-heuristic.json
.claude/scheduled_tasks.lock
.claude/worktrees/**

# Personal docs (not checked in)
docs/ds/


# Committed HTML-era curation pipeline (plan §7/§10.4). These are deliberately TRACKED —
# the reconstruction must be reproducible and served on any checkout / on Railway:
#   history-curation.json            queue          (deterministic prepare; the page's input)
#   history-auto-decisions.json      auto baseline  (cheap-LLM + matcher agreement, pre-fill)
#   history-curation-proposals.json  frontier hints (gpt-5.5 suggestions for uncertain cases)
#   history-decisions.json           human choices  (saved from the curation page; apply input)
#   history-curation-llm-cache.json  resume cache   (prior LLM/frontier asks; lets capped runs continue)
#   history-html-era.json            applied freeze (synced to atlas_history by build:history)
# Regenerated deliberately (reviewed reruns), then committed. The audit/trace write to .cache/.
