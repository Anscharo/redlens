// Ambient declarations for check-govops-census.mjs — only the surface the
// TS side (scripts_tests/govops-uuid-sync.test.ts) imports. Same pattern as
// scripts/lib/*.d.mts: keep in sync with the .mjs exports by hand.
export const DEFINITION_UUIDS: string[];
